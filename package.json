{
  "devDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-refresh": "^0.14.0"
  },
  "scripts": {
    "dev": "clojure -M -m shadow.cljs.devtools.cli watch bootstrap runtime",
    "release": "clojure -M -m shadow.cljs.devtools.cli release bootstrap runtime"
  }
}
